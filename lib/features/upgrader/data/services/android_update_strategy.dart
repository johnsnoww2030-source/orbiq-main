import 'update_strategy.dart';

class AndroidUpdateStrategy implements UpdateStrategy {

  AndroidUpdateStrategy();

  @override
  Future<void> downloadAndInstall(String url) async {
    // پیاده‌سازی لاجیک دانلود و نصب برای اندروید
  }
}